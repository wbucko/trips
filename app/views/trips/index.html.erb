
<div class="header">
<h1>Podróże i wycieczki</h1>
<h2><%= link_to 'New Trip', new_trip_path %></h2>
</div>
<div class="section">
    <% @trips.each do |trip| %>
      <div class="trip_img"><%= link_to image_tag(trip.image_url), trip %></div>
      
    <% end %>



<p id="notice"><%= notice %></p>
</div>