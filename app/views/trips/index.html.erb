

<div class="section">
    <% @trips.each do |trip| %>
      <div class="trip_img"><%= link_to image_tag(trip.image_url), trip %></div>
      
    <% end %>



<p id="notice"><%= notice %></p>
</div>

